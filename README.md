# This ios app is generated by nativescript `tns prepare ios`

## Open the `samplensapp.xcworkspace` in xcode and run the app, it works as nativescript is directly initialised and there is no other view